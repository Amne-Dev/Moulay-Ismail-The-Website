{
  "name": "moulay-ismail-webapp",
  "version": "1.0.0",
  "description": "Dynamic web application for Moulay Ismail High School",
  "main": "index.html",
  "scripts": {
    "start": "netlify dev",
    "build": "echo 'No build step required for this project'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}